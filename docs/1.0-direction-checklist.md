# Ultimate Line Caller 1.0 Direction And Checklist

Last updated: 2026-08-13

## Purpose

This document captures the current 0.1 prototype app state, the intended 1.0 product
direction, and a milestone checklist that can be updated as features are
implemented.

Use this as the working roadmap for turning the current one-game sideline
prototype into a season-aware, tournament-ready, iPad-friendly line-calling and
stats app.

## Product Direction

Ultimate Line Caller 1.0 should stay fast during live points. The primary game
screen should still make it easy to choose seven players, lock a line, and log
the point result with minimal taps. Speed also means predictable responsiveness
on iPad hardware in outdoor field conditions: avoid unnecessary CPU work,
memory growth, storage churn, battery drain, thermal throttling, and anything
that could make the app hang while the game is moving quickly.

Season, tournament, roster, injury, import/export, and analysis features should
support that flow without crowding it. The app should remain local-first and
offline-capable for field use, with explicit backup/export paths so data is not
trapped on one iPad.

## Current 0.1 Prototype State

The app is a compact Vite, React, and TypeScript web app.

- `src/App.tsx` contains the React UI, local component helpers, event handlers,
  modals, and most application flow.
- `src/model.ts` contains shared types, localStorage persistence, game rules,
  line validation, point-context derivation, player counts, and suggested-line
  logic.
- `src/model.test.ts` covers the core model behavior.
- `src/styles.css` contains the full touch-first app layout.
- `index.html` already includes basic iPad web-app metadata.
- `docs/v0.1-prototype/mvp-plan.md`,
  `docs/v0.1-prototype/architecture.md`, and
  `docs/v0.1-prototype/spreadsheet-analysis.md` describe the original
  version 0.1 one-game prototype.

Baseline verification on 2026-08-03:

- `npm test` passed: 12 tests.
- `npm run build` passed.
- Git worktree was clean before this document was added.

Workflow hardening decisions on 2026-08-05:

- GitHub Actions CI is intentionally deferred; use local `npm test` and
  `npm run build` gates until CI is added.
- The local Node.js version is pinned in `.nvmrc`.
- Pull requests should use `.github/pull_request_template.md` to record the
  relevant v1.0 stage, test-first result or justified skip, final verification,
  UI/manual QA notes, and data-safety checks.
- `CHANGELOG.md` is the release-note starting point for v1.0 work.
- Static hosting selection is deferred for now; the product direction remains an
  HTTPS-hosted PWA with no backend for the first field-ready release.
- Real roster exports, backups, injury notes, and stats should be treated as
  sensitive local artifacts and not committed.

Current implemented behavior:

- Create and edit a roster.
- Assign each player as `MMP` or `FMP`.
- Mark players active or inactive.
- Start one active game.
- Configure starting O/D, point-one ratio, target score, and starting field
  side.
- Select a seven-player line.
- Validate line size, ratio, and inactive players.
- Fill a suggested line from lowest point totals.
- Lock a line before logging a point.
- Log whether we scored or the opponent scored.
- Derive score, next point number, next O/D state, required ratio, field side,
  half-time state, and game-over state.
- Track total points played plus O/D points per player.
- Undo the last point.
- Persist the one active game in browser localStorage.
- Show half-time, game-over, and reset confirmations.

Important current limitations:

- The app stores one active game only.
- There is no team, season, tournament, or multi-game model.
- Player `active` is global, not scoped to a tournament or game.
- Deleting a player removes that player from historical point logs, which would
  corrupt season statistics.
- Stats beyond points played, O points, and D points are not tracked.
- There is no roster import/export.
- There is no app backup/restore format.
- There is no CSV export for analysis.
- There is no web manifest, service worker, icon set, offline cache, or
  field-ready install flow yet.
- `package.json` still reports version `0.1.0`; update this when the 1.0 release work begins or when release packaging is formalized.

## 1.0 Design Principles

- Keep line calling fast. Anything not needed during a live point belongs behind
  a drawer, modal, or separate management screen.
- Protect field performance. The live flow should keep main-thread work,
  re-renders, animations, storage writes, and memory growth low enough that
  iPads stay responsive through rapid points and long outdoor games.
- Preserve history. Never delete or rewrite historical game data just because a
  roster changes.
- Prefer scoped availability. A player can be on the season roster, selected
  for a tournament, unavailable for the rest of that tournament, and still
  remain part of long-term team history.
- Store raw events and derive summaries. Point logs and stat events should be
  the source of truth; game, tournament, and season summaries should be derived.
- Stay offline-first. Assume the iPad may have poor connectivity at the field.
- Make backup/export obvious. Local storage is convenient, but not enough by
  itself for valuable season data.
- Avoid a backend until there is a clear need for multi-device sync or shared
  account access.

## Design Skills For 1.0 UI Work

For UI redesign, visual polish, interaction, animation, and iPad/touch work,
use the installed design skills as part of the implementation process:

- Use Hallmark for the app redesign direction, visual audit, design-system
  structure, anti-slop checks, and responsive UI discipline.
- Use Emil design-engineering guidance for component polish, animation judgment,
  press feedback, transitions, and avoiding unnecessary motion.
- Use Apple design guidance for iPad-first touch interaction, immediate feedback,
  interruptible sheets/drawers, spatial consistency, reduced motion, and
  field-ready wayfinding.

The redesign should not turn the live point flow into a showcase. The line
caller still needs to be the fastest path in the app: choose players, lock the
line, log the result, and move to the next point.

## Proposed 1.0 Data Model

Add a release-oriented `AppState` with stable IDs and separate entities. The storage schema can still use an internal schema version that migrates from the 0.1 prototype.

```ts
type AppState = {
  schemaVersion: 2;
  teams: Team[];
  activeTeamId: string | null;
  activeSeasonId: string | null;
  activeTournamentId: string | null;
  activeGameId: string | null;
  lineSuggestionSettings: LineSuggestionSettings;
};

type Team = {
  id: string;
  name: string;
  players: Player[];
  seasons: Season[];
};

type Player = {
  id: string;
  name: string;
  genderCategory: "MMP" | "FMP";
  jerseyNumber?: string;
  notes?: string;
  linePreference?: "offense" | "defense" | "either";
  fieldRole?: "handler" | "cutter" | "hybrid";
  archived: boolean;
};

type Season = {
  id: string;
  name: string;
  teamId: string;
  seasonRosterPlayerIds: string[];
  tournaments: Tournament[];
  createdAt: string;
};

type Tournament = {
  id: string;
  name: string;
  seasonId: string;
  startsAt?: string;
  activeRosterPlayerIds: string[];
  unavailablePlayerIds: string[];
  availabilityEvents: AvailabilityEvent[];
  games: Game[];
};

type Game = {
  id: string;
  tournamentId: string;
  name: string;
  opponent?: string;
  settings: GameSettings;
  pointLog: PointLogEntry[];
  manualHalfTimeTarget: number | null;
  pendingHalfTimeCap: boolean;
  finalized: boolean;
  createdAt: string;
};

type AvailabilityEvent = {
  id: string;
  playerId: string;
  tournamentId: string;
  status: "unavailable" | "available";
  reason?: string;
  createdAt: string;
};

type StatKind = "d" | "score" | "assist" | "throwaway";

type StatEvent = {
  id: string;
  gameId: string;
  pointNumber: number;
  playerId: string;
  kind: StatKind;
  createdAt: string;
};

type LineSuggestionSettings = {
  equalPlayingTime: boolean;
  minHandlers: number;
};
```

Notes:

- Preserve existing `GameSettings`, `PointLogEntry`, ratio, O/D, side, cap, and
  score derivation logic where possible.
- Keep `StatEvent` separate from `PointLogEntry` so stats can be added or fixed
  after the point without changing who played.
- Treat `Player.archived` as a soft delete. Archived players should not appear
  in new roster pickers by default, but their historical stats remain intact.
- Keep equal playing time enabled by default for suggested lines, but allow it
  to be turned off when competitive or tactical selection should take priority.
- Treat `linePreference` and `fieldRole` as suggestion inputs only. The coach
  should always be able to manually override a suggested line.

## Milestone Checklist

### 0. Planning And Baseline

- [x] Analyze the current app state.
- [x] Verify existing test suite.
- [x] Verify production build.
- [x] Capture 1.0 direction and implementation checklist in this document.
- [x] Pin the local Node.js version for consistent installs and builds.
- [x] Add a pull request checklist for v1.0 stage context, verification, manual
  QA, and data-safety review.
- [x] Add local ignore rules for common app export and backup artifacts.
- [ ] Decide when to update `package.json` from `0.1.0` to a formal `1.0.0`.
- [x] Use short-lived feature branches for each implementation stage instead of a long-lived 1.0 branch.
- [ ] Keep `main` as the latest accepted work that should always build and be usable.
- [ ] Cut production releases from tags, for example `v1.0.0`, with GitHub Releases for release notes.
- [x] Add a short changelog or release-notes document.
- [ ] Add GitHub Actions CI for `npm ci`, `npm test`, and `npm run build` on
  pull requests and `main`.

### 1. State Model And Migration

- [x] Add release 1.0 state types.
- [x] Add a new storage key, for example `ultimate-line-caller:v2` or `ultimate-line-caller:1.0`.
- [x] Keep prototype storage loading support.
- [x] Migrate 0.1 prototype state into:
  - [x] one default team
  - [x] one default season
  - [x] one default tournament
  - [x] one active game
- [x] Add tests for prototype-to-1.0 migration.
- [x] Add selectors for active team, active season, active tournament, active
  game, active roster, and unavailable players.
- [x] Replace destructive player deletion with player archiving.
- [x] Add tests proving archived players remain in historical summaries.

### 2. App Structure Refactor

- [ ] Split `src/App.tsx` into focused screen/components.
- [ ] Create a shared app shell/navigation component.
- [ ] Create `GameScreen` for the live line-calling flow.
- [ ] Create `RosterScreen` for team and season roster management.
- [ ] Create `SeasonScreen` for season/tournament/game selection.
- [ ] Create `StatsScreen` for analysis views.
- [ ] Move reusable UI primitives such as `Segmented`, `StatusTile`, and player
  buttons into separate files.
- [ ] Keep current 0.1 prototype behavior unchanged during this refactor.
- [ ] Re-run tests and build after the refactor.

### 3. UI Redesign And Interaction System

- [ ] Run Hallmark pre-flight/audit on the current app before UI edits.
- [ ] Use Hallmark redesign guidance to define the 1.0 visual direction without
  changing the core line-calling workflow.
- [ ] Use Apple design guidance for iPad-first touch targets, immediate
  pointer-down feedback, wayfinding, sheets/drawers, reduced motion, and
  rotation resilience.
- [ ] Use Emil design-engineering guidance for motion restraint, button press
  states, modal/drawer timing, tooltip/popover behavior, and animation review.
- [ ] Establish reusable design tokens for color, type, spacing, radius, motion,
  and focus states.
- [ ] Redesign the live game screen around the fastest sideline path: status,
  selected line, player selection, lock, score result, undo.
- [ ] Redesign management screens for seasons, tournaments, rosters,
  availability, imports/exports, and stats without crowding the live game flow.
- [ ] Add clear active context for team, season, tournament, game, and opponent.
- [ ] Add visible but quiet injured/unavailable states.
- [ ] Verify mobile widths 320, 375, 414, and 768 px plus iPad portrait and
  landscape.
- [ ] Check that text does not overflow, controls do not overlap, and touch
  targets remain comfortable in field use.
- [ ] Add visual/manual QA notes or screenshots for the redesigned flows.
- [ ] Re-run tests, build, and manual sideline flow after the redesign branch.

### 4. Season And Tournament Mode

- [ ] Add create/edit season flow.
- [ ] Add create/edit tournament flow within a season.
- [ ] Add create/edit game flow within a tournament.
- [ ] Add active season/tournament/game switcher.
- [ ] Move existing game setup into the selected tournament/game context.
- [ ] Add game finalization and reopen flow.
- [ ] Add tournament summary derived from all games in the tournament.
- [ ] Add season summary derived from all tournaments in the season.
- [ ] Add tests for game, tournament, and season aggregation.

### 5. Rosters, Active Rosters, Injuries, And Line Preferences

- [ ] Replace global `player.active` with scoped roster/availability logic.
- [ ] Add season roster management.
- [ ] Add tournament active-roster picker from the season roster.
- [ ] Update suggested-line logic to use tournament-active and currently
  available players only.
- [ ] Update line validation to block players who are not on the active
  tournament roster.
- [ ] Add line suggestion settings with equal playing time enabled by default.
- [ ] Add an option to turn off equal-playing-time weighting for suggested
  lines.
- [ ] Add player line preference: offense, defense, or either.
- [ ] Add player field role: handler, cutter, or hybrid.
- [ ] Update suggested-line logic to bias offensive-preference players on O
  points and defensive-preference players on D points.
- [ ] Update suggested-line logic to include at least three handler-capable
  players when roster availability and ratio constraints allow.
- [ ] Add "mark unavailable for tournament" action.
- [ ] Add reason/note for unavailability, for example injury, illness, left
  early, or coach decision.
- [ ] Add undo/reinstate availability action.
- [ ] Add tests for availability and injury behavior.
- [ ] Add tests for line suggestion settings, O/D line preference, and handler
  minimum behavior.
- [ ] Make injured/unavailable status visible but not noisy on the live game
  screen.

### 6. Roster Import And Export

- [ ] Define roster CSV format.
- [ ] Export team roster CSV.
- [ ] Export season roster CSV.
- [ ] Export tournament active roster CSV.
- [ ] Import roster CSV into a team.
- [ ] Import roster CSV into a season roster.
- [ ] Import roster CSV into a tournament active roster.
- [ ] Add duplicate/merge handling by stable ID when present, otherwise
  normalized player name.
- [ ] Add import preview before applying changes.
- [ ] Add tests for parsing, validation, merge, and export formatting.

Suggested roster CSV columns:

```csv
id,name,genderCategory,jerseyNumber,notes
player_abc123,Example Player,MMP,12,
```

### 7. Full Backup And Restore

Schedule note: implement this immediately after the model/migration stage, before
roster imports, stats work, and real tournament use. This remains important even
while hosting and backend work are deferred because browser storage can be
evicted independently of deployment choice.

- [ ] Define full-app JSON export format.
- [ ] Add export backup action.
- [ ] Add import backup action.
- [ ] Validate backup version before import.
- [ ] Show backup summary before restore.
- [ ] Add "replace current data" and "merge into current data" decisions.
- [ ] Add tests for backup export/import.
- [ ] Add visible backup reminder before tournament/season use.

### 8. Stats Capture

- [ ] Add `StatEvent` model.
- [ ] Add stat-event creation and deletion helpers.
- [ ] Add quick stats drawer after logging a point.
- [ ] Support assigning `Score`, `Assist`, `D`, and `Throwaway`.
- [ ] Prefer players from the just-played line when assigning stats.
- [ ] Allow skipping stats quickly.
- [ ] Allow editing stats from the point log.
- [ ] Decide whether to enforce common constraints, such as one score per point
  where we scored, or allow flexible correction.
- [ ] Add tests for stat capture and editing.

### 9. Stats Views And Percentages

- [ ] Add game-level stats table.
- [ ] Add tournament-level stats table.
- [ ] Add season-level stats table.
- [ ] Show points played, O points, D points, scores, assists, Ds, and
  throwaways.
- [ ] Show per-point rates for each stat.
- [ ] Show O hold rate: O points where we scored divided by O points played.
- [ ] Show D break rate: D points where we scored divided by D points played.
- [ ] Show share of team scores, assists, Ds, and throwaways.
- [ ] Add sorting by name, points, rates, and each stat.
- [ ] Add tests for percentage calculations.

### 10. Stats Export

- [ ] Export raw point log CSV.
- [ ] Export raw stat events CSV.
- [ ] Export game summary CSV.
- [ ] Export tournament summary CSV.
- [ ] Export season summary CSV.
- [ ] Include stable IDs plus display names in exports.
- [ ] Add tests for export formatting.

Suggested summary CSV columns:

```csv
scope,scopeName,playerId,playerName,genderCategory,points,oPoints,dPoints,scores,assists,ds,throwaways,oHoldRate,dBreakRate
```

Suggested raw stat-event CSV columns:

```csv
gameId,gameName,pointNumber,playerId,playerName,kind,createdAt
```

### 11. Live Game UI Improvements

- [ ] Preserve the sticky score/status band.
- [ ] Keep "We Scored" and "Opp Scored" as large primary actions.
- [ ] Keep line selection grouped by `MMP` and `FMP`.
- [ ] Add compact context for active season, tournament, game, and opponent.
- [ ] Add quick switcher for management screens without disrupting the live
  point flow.
- [ ] Add compact unavailable/injured indicators.
- [ ] Add compact controls for suggestion mode, including equal playing time on
  or off.
- [ ] Make player line preference and handler/cutter role visible in roster
  management without slowing down live point selection.
- [ ] Add quick stat capture without requiring stats before the next point.
- [ ] Keep line buttons large enough for sideline iPad use.
- [ ] Test on iPad portrait and landscape sizes.
- [ ] Add a manual sideline test script.

### 12. iPad Deployment And Field Readiness

- [ ] Add `manifest.webmanifest`.
- [ ] Add Apple touch icons and app icons.
- [ ] Add service worker for offline app shell caching.
- [ ] Add offline fallback behavior.
- [ ] Add visible app version/build identifier.
- [ ] Add offline-ready status.
- [ ] Add backup/export prompt before first real tournament use.
- [ ] Document "install to Home Screen from Safari" workflow.
- [ ] Choose initial static hosting path.
- [ ] Test installed Home Screen app on an iPad.
- [ ] Test reload, airplane mode, and app relaunch during a fake game.
- [ ] Decide whether TestFlight/Capacitor is needed after PWA field testing.

Recommended deployment path:

1. Ship 1.0 first as a static HTTPS-hosted PWA.
2. Install it on iPads from Safari using Add to Home Screen / Open as Web App.
3. Keep all data local and offline, with explicit backup/export.
4. Only move to Capacitor/TestFlight if native packaging becomes necessary.
5. Only add backend sync if multi-device shared state becomes necessary.

Deployment notes:

- Apple supports adding websites to the iPad Home Screen.
- Newer Safari/WebKit releases support more capable installed web apps, but
  explicit manifest, icons, and service worker support are still useful.
- Browser storage can be evicted under some conditions, so export/backup cannot
  be optional for valuable season data.
- TestFlight is a good later option for a native wrapper.
- MDM or enterprise distribution is only worth considering for managed team or
  organization-owned iPads.

### 13. Quality Gates

- [x] Existing 0.1 prototype tests remain passing.
- [ ] Hallmark redesign/audit pass is complete for the 1.0 UI.
- [ ] Emil-style motion/component review is complete for redesigned interactions.
- [ ] iPad/touch interaction review is complete for redesigned flows.
- [x] Migration tests pass.
- [ ] Aggregation tests pass.
- [ ] Import/export tests pass.
- [x] Build passes.
- [ ] Manual one-game sideline flow passes.
- [ ] Manual tournament flow passes.
- [ ] Manual backup/restore flow passes.
- [ ] Manual iPad offline flow passes.
- [x] No historical stats are lost when rosters change.

## Open Decisions

- [ ] Should 1.0 support multiple teams immediately, or keep the UI single-team
  while the data model supports multiple teams?
- [ ] Should stats enforce ultimate-specific constraints, or allow flexible
  entry and correction?
- [ ] Should injuries be named "injury" specifically, or generalized as
  "unavailable" with a reason?
- [ ] Should games support opponent names, tournament round labels, and field
  numbers in 1.0?
- [ ] Should exports target CSV only at first, or also generate spreadsheet-ready
  multi-sheet workbooks?
- [ ] Which hosting target should be used for the first PWA field test?
- [ ] Is native packaging via Capacitor/TestFlight needed for 1.0, or should it
  wait until after PWA testing?
- [ ] Should equal-playing-time-off mode use only player preferences and roles
  for v1.0, or should a later depth chart become the stronger ordering input?

## Post-1.0 Backlog

- [ ] Add depth chart or priority ranking fields for suggested lines when equal
  playing time is turned off.
- [ ] Add zone-defense position attributes for players.
- [ ] Add a zone-defense suggestion mode that can build D lines around the
  desired zone positions when the team plans to run zone.
- [ ] Define a field-performance budget for target iPad hardware, including
  acceptable tap latency, long-game memory growth, storage-write cadence,
  battery use, and thermal behavior.
- [ ] Profile and optimize the live game flow on real iPads during long fake-game
  sessions so selection, line locking, point logging, undo, and quick stats stay
  responsive outdoors.

## Suggested Build Order And Branches

Each stage should be implemented on its own short-lived feature branch, reviewed/tested, and merged back to `main`. Production releases should be cut from tags on `main`, not from a long-lived `v1.0` branch.

1. Model and migration: `feature/1.0-model-migration`.
2. Full backup and restore foundation: `feature/1.0-backup-restore`.
3. Component/screen refactor with no behavior change: `feature/1.0-app-structure`.
4. UI redesign and interaction system: `feature/1.0-ui-redesign`.
5. Season/tournament/game selection: `feature/1.0-season-mode`.
6. Scoped roster availability, injuries, and line preferences: `feature/1.0-roster-availability`.
7. Roster import/export: `feature/1.0-roster-import-export`.
8. Stats event capture: `feature/1.0-stats-capture`.
9. Stats summaries and CSV export: `feature/1.0-stats-exports`.
10. PWA install/offline hardening: `feature/1.0-pwa-field-ready`.
11. iPad field test fixes: `feature/1.0-field-test-fixes`.

Release flow:

1. Merge each completed stage into `main` after tests/build pass.
2. Keep `main` deployable as the latest accepted build.
3. When 1.0 is field-ready, tag `main` as `v1.0.0`.
4. Create a GitHub Release for `v1.0.0` with user-facing release notes.
5. Deploy the PWA from the release tag/build.
6. If production needs patches after `main` has moved on, create a maintenance branch such as `maintenance/1.0`, tag fixes as `v1.0.1`, and merge or cherry-pick the fix back to `main`.

This order keeps risk low: first make the state capable of representing 1.0,
then separate the app into workable screens, establish the redesigned UI system,
then add management screens, live-game features, and deployment hardening.
